---
- name: Setup system
  hosts: localhost
  connection: local
  remote_user: "{{ user }}"
  ansible_become_pass: "{{ password }}"
  become: yes
  
  tasks:
    - import_tasks: tasks/packages.yml
    - import_tasks: tasks/configs.yml
